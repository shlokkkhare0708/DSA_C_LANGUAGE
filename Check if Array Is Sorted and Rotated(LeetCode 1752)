#include <stdbool.h>

bool check(int* nums, int numsSize) {
    int dip = 0;
    int n = numsSize;

    for (int i = 0; i < n; i++) {
        if (nums[i] > nums[(i + 1) % n]) {
            dip++;
        }
    }

    if (dip <= 1) {
        return true;
    } else {
        return false;
    }
}
